integration:
  webhooks:
    download_file_webhook:
      on:
        - DownloadFileComplete
      call:
        url: web-graffiti:8080/slskd_webhook
      timeout: 5000
      retry:
        attempts: 3